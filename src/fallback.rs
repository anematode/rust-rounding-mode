
/// Computes a rounded multiplication upward of two double-precision floating point numbers.
/// Assumes the current rounding mode is round-to-nearest, ties-to-even.
pub fn multiply_round_up(a: f64, b: f64) -> f64 {
    return a * b;
}
